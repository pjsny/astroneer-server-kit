name: Stop Server

on:
  workflow_dispatch:
  workflow_call:

jobs:
  stop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: "1.7.0"

      - name: Terraform Init
        working-directory: terraform/scaleway
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.SCW_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.SCW_SECRET_KEY }}
        run: terraform init

      - name: Check if server exists
        id: check
        working-directory: terraform/scaleway
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.SCW_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.SCW_SECRET_KEY }}
          TF_VAR_scw_access_key: ${{ secrets.SCW_ACCESS_KEY }}
          TF_VAR_scw_secret_key: ${{ secrets.SCW_SECRET_KEY }}
          TF_VAR_scw_project_id: ${{ secrets.SCW_PROJECT_ID }}
          TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
          TF_VAR_repo_url: ${{ github.server_url }}/${{ github.repository }}
        run: |
          RESULT=$(terraform state show scaleway_instance_server.astro 2>/dev/null && echo "exists" || echo "none")
          echo "server=$RESULT" >> $GITHUB_OUTPUT

      - name: Get server IP
        if: steps.check.outputs.server == 'exists'
        id: ip
        working-directory: terraform/scaleway
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.SCW_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.SCW_SECRET_KEY }}
        run: |
          IP=$(terraform output -raw server_ip 2>/dev/null || echo "")
          echo "ip=$IP" >> $GITHUB_OUTPUT

      - name: Gracefully stop Astroneer before destroy
        if: steps.check.outputs.server == 'exists' && steps.ip.outputs.ip != ''
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh -o StrictHostKeyChecking=no Administrator@${{ steps.ip.outputs.ip }} \
            "powershell -Command Stop-Service astroneer" || true

      - name: Terraform Destroy (keeps saves volume)
        if: steps.check.outputs.server == 'exists'
        working-directory: terraform/scaleway
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.SCW_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.SCW_SECRET_KEY }}
          TF_VAR_scw_access_key: ${{ secrets.SCW_ACCESS_KEY }}
          TF_VAR_scw_secret_key: ${{ secrets.SCW_SECRET_KEY }}
          TF_VAR_scw_project_id: ${{ secrets.SCW_PROJECT_ID }}
          TF_VAR_ssh_public_key: ${{ secrets.SSH_PUBLIC_KEY }}
          TF_VAR_repo_url: ${{ github.server_url }}/${{ github.repository }}
        run: |
          terraform destroy -auto-approve \
            -target=scaleway_instance_server.astro \
            -target=scaleway_instance_security_group.astro \
            -target=scaleway_account_ssh_key.astro

      - name: Summary
        run: |
          echo "## Server stopped" >> $GITHUB_STEP_SUMMARY
          echo "World saves are safe on the persistent volume." >> $GITHUB_STEP_SUMMARY
          echo "Run **Start Server** to bring it back up." >> $GITHUB_STEP_SUMMARY

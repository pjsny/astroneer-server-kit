# Bootstrap is now handled automatically by bin/setup.
# The Hetzner Object Storage bucket (astro-server-tf-state) is created
# via the AWS CLI in that script — the hcloud Terraform provider does
# not manage object storage resources.
#
# You do not need to run anything in this directory manually.
# Just run: bash bin/setup

# Local env — never commit credentials
.env

# Bun
node_modules/
bun.lockb

# Server saves and generated files
saves/
*.sav
*.tmp
astro-server-bin/

# Terraform (local plugin cache — lock file is committed)
.terraform/
terraform/bootstrap/.terraform/
terraform/bootstrap/*.tfstate
terraform/bootstrap/*.tfstate.*
